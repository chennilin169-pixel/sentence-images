# Sentence → Images

输入一句话，生成指定尺寸/字体/位置的纯文字 PNG 图片。

## Run

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Font

建议把字体放到 `assets/fonts/`，然后在页面里填写路径，例如：
`assets/fonts/NotoSansSC-Regular.ttf`
